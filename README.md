# ALA Brand Page

Static brand and FAQ page for ALA.

## Project Structure

- `index.html` - page markup
- `styles.css` - responsive styles and animations
- `script.js` - navigation and reveal interactions
- `assets/` - images, icons, video, and fonts

## Local Preview

Open `index.html` directly in a browser, or serve the folder with any static server.

## GitHub Pages

This project is designed to be published from the `main` branch root using GitHub Pages.

After pushing to GitHub:

1. Open the repository on GitHub.
2. Go to `Settings` -> `Pages`.
3. Under `Build and deployment`, set `Source` to `Deploy from a branch`.
4. Select branch `main` and folder `/ (root)`.
5. Save.

The public URL will usually be:

```text
https://<your-github-username>.github.io/<repository-name>/
```

## Updating

After editing files:

```bash
git add .
git commit -m "Update site"
git push
```
